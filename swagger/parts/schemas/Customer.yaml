type: object
required:
  - rate
properties:
  rate:
    type: string
    description: Percentual dos clientes de cada faixa relativa ao serviço ofertado, para pessoa natural informado no período, conforme Res nº32, BCB, 2020.
    pattern: ^[0-9](\.[0-9]{4})$|^NA$
    example: "0.1500"
    maxLength: 6
